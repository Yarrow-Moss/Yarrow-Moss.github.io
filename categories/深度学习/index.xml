<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
    <channel>
        <title>深度学习 on 雅尼克.莫(Yannick.Mo)</title>
        <link>https://Yannick-Mo.github.io/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/</link>
        <description>Recent content in 深度学习 on 雅尼克.莫(Yannick.Mo)</description>
        <generator>Hugo -- gohugo.io</generator>
        <language>zh-cn</language>
        <copyright>雅尼克.莫(Yannick.Mo)</copyright>
        <lastBuildDate>Wed, 29 Jul 2026 13:51:28 +0800</lastBuildDate><atom:link href="https://Yannick-Mo.github.io/categories/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/index.xml" rel="self" type="application/rss+xml" /><item>
        <title>标准Transformer完整架构流程笔记</title>
        <link>https://Yannick-Mo.github.io/p/%E6%A0%87%E5%87%86transformer%E5%AE%8C%E6%95%B4%E6%9E%B6%E6%9E%84%E6%B5%81%E7%A8%8B%E7%AC%94%E8%AE%B0/</link>
        <pubDate>Wed, 29 Jul 2026 13:51:28 +0800</pubDate>
        
        <guid>https://Yannick-Mo.github.io/p/%E6%A0%87%E5%87%86transformer%E5%AE%8C%E6%95%B4%E6%9E%B6%E6%9E%84%E6%B5%81%E7%A8%8B%E7%AC%94%E8%AE%B0/</guid>
        <description>&lt;h1 id=&#34;-标准transformer原版完整架构流程笔记&#34;&gt;📘 标准Transformer（原版）完整架构流程笔记
&lt;/h1&gt;&lt;h2 id=&#34;适用场景与宏观哲学&#34;&gt;适用场景与宏观哲学
&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;任务类型&lt;/strong&gt;：Seq2Seq（如英译中）。输入是一整个源句子（如英文），输出是逐个生成的目标句子（如中文）。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;宏观三件套&lt;/strong&gt;：
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;编码器（Encoder）&lt;/strong&gt;：**双向（Bi-directional）**读取整个源句子，将其提炼为&amp;quot;记忆向量&amp;quot;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;解码器（Decoder）&lt;/strong&gt;：**自回归（Auto-regressive）**从左到右生成目标句子，生成时通过&amp;quot;交叉注意力&amp;quot;不断查阅编码器的记忆。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;共享桥梁&lt;/strong&gt;：解码器最后一层输出 -&amp;gt; 线性层（Linear）-&amp;gt; Softmax -&amp;gt; 输出概率分布。&lt;/li&gt;
&lt;/ol&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2 id=&#34;第一阶段输入嵌入与位置编码input-preparation&#34;&gt;第一阶段：输入嵌入与位置编码（Input Preparation）
&lt;/h2&gt;&lt;p&gt;&lt;strong&gt;这一步编码器和解码器完全一致，但输入内容不同（源语言 vs 目标语言）。&lt;/strong&gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;词元嵌入（Token Embedding）&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;将输入Token ID转化为密集向量 &lt;code&gt;X_emb&lt;/code&gt;，形状 &lt;code&gt;(Batch, Seq_Len, d_model)&lt;/code&gt;（&lt;code&gt;d_model&lt;/code&gt; 通常为512或1024）。&lt;/li&gt;
&lt;li&gt;乘以 &lt;code&gt;sqrt(d_model)&lt;/code&gt; 进行缩放（原论文操作，为了平衡嵌入与位置编码的量级）。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;位置编码（Positional Encoding）&lt;/strong&gt;——&lt;strong&gt;这里是标准写法，而非RoPE&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;不使用可学习嵌入，而是直接&lt;strong&gt;加法&lt;/strong&gt;加入固定公式计算的向量。&lt;/li&gt;
&lt;li&gt;公式：对于位置 &lt;code&gt;pos&lt;/code&gt; 和维度索引 &lt;code&gt;i&lt;/code&gt;：
&lt;ul&gt;
&lt;li&gt;偶数位：&lt;code&gt;PE(pos, 2i) = sin(pos / 10000^(2i/d_model))&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;奇数位：&lt;code&gt;PE(pos, 2i+1) = cos(pos / 10000^(2i/d_model))&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;最终输入向量&lt;/strong&gt;：&lt;code&gt;X = X_emb + PE&lt;/code&gt;（形状不变）。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;hr&gt;
&lt;h2 id=&#34;第二阶段编码器encoder-理解源语言&#34;&gt;第二阶段：编码器（Encoder）—— 理解源语言
&lt;/h2&gt;&lt;h3 id=&#34;前置设定&#34;&gt;前置设定
&lt;/h3&gt;&lt;p&gt;假设输入到这一层的张量为 &lt;strong&gt;X&lt;/strong&gt;，形状为 &lt;code&gt;(B, S, d_model)&lt;/code&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;B&lt;/strong&gt;：批次大小&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;S&lt;/strong&gt;：源语言序列长度（比如句子有10个词）&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;d_model&lt;/strong&gt;：模型维度（原论文为512）&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;关键点&lt;/strong&gt;：编码器是&lt;strong&gt;双向&lt;/strong&gt;的，即计算某个位置的注意力时，它可以看到序列中&lt;strong&gt;所有其他位置&lt;/strong&gt;（包括它自己以及后面的词），不存在掩码。&lt;/p&gt;
&lt;hr&gt;
&lt;h3 id=&#34;子层一多头自注意力子层multi-head-self-attention-sub-layer&#34;&gt;子层一：多头自注意力子层（Multi-Head Self-Attention Sub-layer）
&lt;/h3&gt;&lt;p&gt;本子层遵循&amp;quot;核心注意力 → Add → LayerNorm&amp;quot;的流程。&lt;/p&gt;
&lt;h4 id=&#34;步骤-21多头自注意力multi-head-self-attention&#34;&gt;步骤 2.1：多头自注意力（Multi-Head Self-Attention）
&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;做什么&lt;/strong&gt;：让每个位置的信息去&amp;quot;询问&amp;quot;所有位置，并聚合全局上下文。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;拆解动作&lt;/strong&gt;：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;生成 Q、K、V&lt;/strong&gt;&lt;br&gt;
X 会分别乘以三个权重矩阵 &lt;code&gt;W_Q, W_K, W_V&lt;/code&gt;（每个形状均为 &lt;code&gt;d_model × d_model&lt;/code&gt;），得到 Q、K、V，形状均为 &lt;code&gt;(B, S, d_model)&lt;/code&gt;。&lt;br&gt;
这里 Q、K、V 来自&lt;strong&gt;同一个输入 X&lt;/strong&gt;，所以叫&amp;quot;自&amp;quot;注意力。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;拆分为多头&lt;/strong&gt;&lt;br&gt;
将 &lt;code&gt;d_model&lt;/code&gt; 沿特征维度切分成 &lt;strong&gt;h 个头&lt;/strong&gt;（原论文 h=8，每头维度 &lt;code&gt;d_k = d_model / h = 64&lt;/code&gt;）。&lt;br&gt;
此时 Q、K、V 的形状变为 &lt;code&gt;(B, h, S, d_k)&lt;/code&gt;。每个头独立负责一个子空间，去捕捉不同层面的关系（语法、语义、位置等）。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;计算注意力分数&lt;/strong&gt;&lt;br&gt;
对每个头，计算 &lt;code&gt;Q × K^T&lt;/code&gt;（最后两维矩阵乘法），得到分数矩阵，形状 &lt;code&gt;(B, h, S, S)&lt;/code&gt;。&lt;br&gt;
其中第 &lt;code&gt;i&lt;/code&gt; 行第 &lt;code&gt;j&lt;/code&gt; 列的值表示：&lt;strong&gt;第 i 个位置对第 j 个位置的关注强度（未归一化）&lt;/strong&gt;。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;缩放（Scaling）&lt;/strong&gt;&lt;br&gt;
将分数除以 &lt;code&gt;√d_k&lt;/code&gt;（即√64=8）。&lt;strong&gt;目的&lt;/strong&gt;：防止点积值过大，导致后续 Softmax 进入梯度极小的饱和区。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Softmax 归一化&lt;/strong&gt;&lt;br&gt;
对分数矩阵的&lt;strong&gt;每一行&lt;/strong&gt;（即每个查询位置）做 Softmax，使每一行的注意力权重之和为 1。&lt;br&gt;
此时矩阵中每个值表示：&lt;strong&gt;当前查询位置应该给每个键位置分配的注意力比例&lt;/strong&gt;。因为没有掩码，所有位置的权重都参与计算。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;加权求和&lt;/strong&gt;&lt;br&gt;
将权重矩阵与 V 相乘：&lt;code&gt;Attention_Out = Softmax(···) × V&lt;/code&gt;，形状回到 &lt;code&gt;(B, h, S, d_k)&lt;/code&gt;。&lt;br&gt;
这一步得到的是&lt;strong&gt;每个头&lt;/strong&gt;加权聚合后的特征。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;合并多头&lt;/strong&gt;&lt;br&gt;
将 h 个头在特征维度上拼接（Concat），得到 &lt;code&gt;(B, S, d_model)&lt;/code&gt;，再乘以输出投影矩阵 &lt;code&gt;W_O&lt;/code&gt;（&lt;code&gt;d_model × d_model&lt;/code&gt;），得到最终的多头输出 &lt;code&gt;Attn_Out&lt;/code&gt;，形状 &lt;code&gt;(B, S, d_model)&lt;/code&gt;。&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;此时得到的 Attn_Out&lt;/strong&gt;：每个位置的向量都已经融合了全序列其他位置的信息，但它是&lt;strong&gt;线性变换&lt;/strong&gt;后的结果，还没与原始输入交互。&lt;/p&gt;
&lt;hr&gt;
&lt;h4 id=&#34;步骤-22残差连接add&#34;&gt;步骤 2.2：残差连接（Add）
&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;做什么&lt;/strong&gt;：将原始输入 X 与刚刚算出的 Attn_Out 直接相加：&lt;code&gt;X = X + Attn_Out&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;为什么必须这么做&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;梯度高速公路&lt;/strong&gt;：反向传播时，梯度可以直接通过&amp;quot;加&amp;quot;这条路径流回浅层，避免深层网络梯度消失。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;保留原始信息&lt;/strong&gt;：注意力层可能过度拟合或丢失某些局部特征，加上原始 X 相当于给模型一个&amp;quot;保底&amp;quot;选项——如果注意力没用，它至少还能保留输入本身。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;注意&lt;/strong&gt;：原版 Transformer 在这里是&lt;strong&gt;先 Add 后 LayerNorm&lt;/strong&gt;（即 Post-Norm 结构），这与现在流行的 Pre-Norm（先 LN 后 Add）顺序相反。Post-Norm 训练时对学习率更敏感，但原论文就是这么设计的。&lt;/p&gt;
&lt;hr&gt;
&lt;h4 id=&#34;步骤-23层归一化layernorm&#34;&gt;步骤 2.3：层归一化（LayerNorm）
&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;做什么&lt;/strong&gt;：对 &lt;code&gt;X = X + Attn_Out&lt;/code&gt; 这个结果做归一化。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;细节&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;对每个样本的每个位置，计算其 &lt;code&gt;d_model&lt;/code&gt; 维特征上的&lt;strong&gt;均值&lt;/strong&gt;和&lt;strong&gt;方差&lt;/strong&gt;。&lt;/li&gt;
&lt;li&gt;然后用公式 &lt;code&gt;(X - mean) / √(var + ε)&lt;/code&gt; 标准化，再乘以可学习的缩放参数 &lt;code&gt;γ&lt;/code&gt; 和平移参数 &lt;code&gt;β&lt;/code&gt;（形状均为 &lt;code&gt;d_model&lt;/code&gt;）。&lt;/li&gt;
&lt;li&gt;输出形状仍为 &lt;code&gt;(B, S, d_model)&lt;/code&gt;。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;它与 BatchNorm 的区别&lt;/strong&gt;：LayerNorm 在&lt;strong&gt;特征维度&lt;/strong&gt;上独立归一化，不受批次大小和序列长度影响，非常适合变长序列（NLP 场景）。&lt;br&gt;
&lt;strong&gt;作用&lt;/strong&gt;：将数据分布拉回均值为 0、方差为 1 的稳定区间，让后续 FFN 层的输入不至于过于爆炸或萎缩，加速收敛。&lt;/p&gt;
&lt;hr&gt;
&lt;h3 id=&#34;子层二前馈网络子层feed-forward-network-sub-layer&#34;&gt;子层二：前馈网络子层（Feed-Forward Network Sub-layer）
&lt;/h3&gt;&lt;p&gt;本子层遵循&amp;quot;核心 FFN → Add → LayerNorm&amp;quot;的流程。注意，此处的输入 X 已经是子层一（经过 Add+LN）的输出。&lt;/p&gt;
&lt;h4 id=&#34;步骤-24前馈网络ffn&#34;&gt;步骤 2.4：前馈网络（FFN）
&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;做什么&lt;/strong&gt;：对每个位置&lt;strong&gt;独立&lt;/strong&gt;地进行两次线性变换，中间夹一个 ReLU 激活。这是逐位置（Position-wise）的全连接层。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;公式&lt;/strong&gt;：&lt;code&gt;FFN(X) = ReLU(X * W_1 + b_1) * W_2 + b_2&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;张量变化&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;第一层：&lt;code&gt;X (B, S, 512)&lt;/code&gt; 乘以 &lt;code&gt;W_1 (512 × 2048)&lt;/code&gt;，加上偏置，得到 &lt;code&gt;(B, S, 2048)&lt;/code&gt;。&lt;strong&gt;内层维度扩张到 4 倍&lt;/strong&gt;，这是原论文的经典设置。&lt;/li&gt;
&lt;li&gt;激活函数：ReLU 将负数全部置为 0，引入&lt;strong&gt;非线性&lt;/strong&gt;，让模型能拟合复杂函数。同时 ReLU 计算简单，能缓解梯度消失。&lt;/li&gt;
&lt;li&gt;第二层：&lt;code&gt;(B, S, 2048)&lt;/code&gt; 乘以 &lt;code&gt;W_2 (2048 × 512)&lt;/code&gt;，加偏置，投影回 &lt;code&gt;(B, S, 512)&lt;/code&gt;。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;关键点&lt;/strong&gt;：这个 FFN 对每个位置（S 个）独立运算，参数在所有位置共享。它相当于在注意力层&amp;quot;全局混合&amp;quot;之后，再对每个位置做&amp;quot;深度加工&amp;quot;——注意力负责&amp;quot;看哪里&amp;quot;，FFN 负责&amp;quot;想什么&amp;quot;（提炼更高阶的特征）。&lt;/p&gt;
&lt;hr&gt;
&lt;h4 id=&#34;步骤-25残差连接add&#34;&gt;步骤 2.5：残差连接（Add）
&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;做什么&lt;/strong&gt;：将步骤 2.3 归一化后的输出（即 &lt;code&gt;LayerNorm( X + Attn_Out )&lt;/code&gt;）与 FFN 的输出相加：&lt;code&gt;X = X + FFN_Out&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;这里的 &lt;code&gt;X&lt;/code&gt; 实际上是&lt;strong&gt;经过第一次残差+归一化后的结果&lt;/strong&gt;（即子层一的输出），而不是最原始的输入。&lt;br&gt;
&lt;strong&gt;作用&lt;/strong&gt;：与步骤 2.2 相同，给 FFN 层也提供一条梯度直通捷径，确保即使 FFN 学习效果不佳，信息仍能无损传递。&lt;/p&gt;
&lt;hr&gt;
&lt;h4 id=&#34;步骤-26层归一化layernorm&#34;&gt;步骤 2.6：层归一化（LayerNorm）
&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;做什么&lt;/strong&gt;：对步骤 2.5 相加的结果再次做 LayerNorm，得到本编码器层的最终输出，形状仍为 &lt;code&gt;(B, S, d_model)&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;这是 Post-Norm 的典型收尾：&lt;strong&gt;每个子层（Attention 或 FFN）都遵循&amp;quot;先 Add 后 Norm&amp;quot;&lt;/strong&gt;。&lt;br&gt;
至此，一个编码器层的全部计算结束。&lt;/p&gt;
&lt;hr&gt;
&lt;h3 id=&#34;额外补充原论文中未列在你这张表里但实际存在的细节&#34;&gt;额外补充（原论文中未列在你这张表里，但实际存在的细节）
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Dropout&lt;/strong&gt;：在每一步残差相加之前（即 Attn_Out 和 FFN_Out 输出后），都会施加 Dropout（通常 p=0.1），用于正则化，防止过拟合。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;位置编码&lt;/strong&gt;：在进入第一个编码器层之前，输入词嵌入已经加上了位置编码（正弦/余弦或可学习）。但每层内部不再额外加位置信息，因为多头注意力本身是置换不变的（打乱顺序点积结果不变），位置信息全靠输入时的那一次注入。&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h3 id=&#34;这一层的最终产出与下游关系&#34;&gt;这一层的最终产出与下游关系
&lt;/h3&gt;&lt;p&gt;本层输出 &lt;code&gt;X&lt;/code&gt; 会作为&lt;strong&gt;下一层编码器&lt;/strong&gt;的输入，重复上述 6 次。&lt;br&gt;
当所有 6 层跑完后，最终得到的张量被称为 &lt;strong&gt;Encoder_Memory&lt;/strong&gt;，形状 &lt;code&gt;(B, S_src, d_model)&lt;/code&gt;。&lt;br&gt;
它的每一行都包含了源语言句子中&lt;strong&gt;该词与所有其他词交互后的全局上下文表示&lt;/strong&gt;。&lt;br&gt;
这个 Memory 将作为&amp;quot;钥匙&amp;quot;和&amp;quot;值&amp;quot;（即 K 和 V），送入解码器的**交叉注意力（Cross-Attention）**层，让解码器在生成目标语言时，能够&amp;quot;查阅&amp;quot;整个源句子的信息。&lt;/p&gt;
&lt;hr&gt;
&lt;h2 id=&#34;第三阶段解码器decoder-生成目标语言&#34;&gt;第三阶段：解码器（Decoder）—— 生成目标语言
&lt;/h2&gt;&lt;h3 id=&#34;前置设定-1&#34;&gt;前置设定
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;解码器输入&lt;/strong&gt;：不是原始目标句子，而是**目标句子往右偏移一位（Shifted Right）**的嵌入 + 位置编码。比如要翻译&amp;quot;I love you&amp;quot;，输入是&amp;quot;&lt;code&gt;&amp;lt;BOS&amp;gt;&lt;/code&gt; I love&amp;quot;（&lt;code&gt;&amp;lt;BOS&amp;gt;&lt;/code&gt;是起始符），目标是预测&amp;quot;I love you &lt;code&gt;&amp;lt;EOS&amp;gt;&lt;/code&gt;&amp;quot;。这样第 &lt;code&gt;t&lt;/code&gt; 个位置的输入，恰好对应第 &lt;code&gt;t-1&lt;/code&gt; 个位置的真实词，强行建立了因果依赖。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;张量&lt;/strong&gt;：记本层输入为 &lt;code&gt;X_dec&lt;/code&gt;，形状 &lt;code&gt;(B, T, d_model)&lt;/code&gt;，其中 &lt;code&gt;T&lt;/code&gt; 是目标序列当前长度（训练时是整句并行，推理时逐个增长）。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;记忆&lt;/strong&gt;：编码器最终输出的 &lt;code&gt;Encoder_Memory&lt;/code&gt; 形状 &lt;code&gt;(B, S, d_model)&lt;/code&gt;，在整个解码过程中只读不变。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;核心灵魂&lt;/strong&gt;：解码器是**自回归（Autoregressive）**的——生成目标语言时，&lt;strong&gt;当前位置只能看到它自己以及之前已生成的位置，绝对不能偷看未来的词&lt;/strong&gt;。这是它和编码器最根本的区别。&lt;/p&gt;
&lt;p&gt;解码器同样堆叠 &lt;code&gt;N=6&lt;/code&gt; 层，每一层都按以下&lt;strong&gt;三个子层&lt;/strong&gt;串行处理（每个子层都遵循&amp;quot;先 Add 后 LayerNorm&amp;quot;的 Post-Norm 结构）。&lt;/p&gt;
&lt;hr&gt;
&lt;h3 id=&#34;子层一带掩码的多头自注意力子层masked-multi-head-self-attention-sub-layer&#34;&gt;子层一：带掩码的多头自注意力子层（Masked Multi-Head Self-Attention Sub-layer）
&lt;/h3&gt;&lt;p&gt;本子层遵循&amp;quot;核心注意力 → Add → LayerNorm&amp;quot;的流程。&lt;/p&gt;
&lt;h4 id=&#34;步骤-31带掩码的多头自注意力masked-multi-head-self-attention&#34;&gt;步骤 3.1：带掩码的多头自注意力（Masked Multi-Head Self-Attention）
&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;做什么&lt;/strong&gt;：让目标语言内部各个位置互相看，但&lt;strong&gt;必须戴上&amp;quot;因果掩码（Causal Mask）&amp;quot;&lt;/strong&gt;，确保第 &lt;code&gt;i&lt;/code&gt; 个位置只能看到 &lt;code&gt;0&lt;/code&gt; 到 &lt;code&gt;i&lt;/code&gt; 的位置，看不到 &lt;code&gt;i+1&lt;/code&gt; 及以后。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;拆解动作&lt;/strong&gt;：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;生成 Q、K、V&lt;/strong&gt;：和编码器一样，&lt;code&gt;X_dec&lt;/code&gt; 分别乘以当前子层的&lt;code&gt;W_Q, W_K, W_V&lt;/code&gt;，得到 Q、K、V，形状均为 &lt;code&gt;(B, T, d_model)&lt;/code&gt;。拆成 h 个头后，形状为 &lt;code&gt;(B, h, T, d_k)&lt;/code&gt;。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;计算未缩放分数&lt;/strong&gt;：&lt;code&gt;Q × K^T&lt;/code&gt;，得到分数矩阵形状 &lt;code&gt;(B, h, T, T)&lt;/code&gt;。&lt;br&gt;
此时第 &lt;code&gt;i&lt;/code&gt; 行第 &lt;code&gt;j&lt;/code&gt; 列，代表目标序列第 &lt;code&gt;i&lt;/code&gt; 个位置对第 &lt;code&gt;j&lt;/code&gt; 个位置的原始关注度。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;应用上三角掩码（关键步骤）&lt;/strong&gt;：&lt;br&gt;
生成一个 &lt;code&gt;(T, T)&lt;/code&gt; 的上三角矩阵（对角线以上为 &lt;code&gt;1&lt;/code&gt;，含对角线及以下为 &lt;code&gt;0&lt;/code&gt;）。&lt;br&gt;
将这个矩阵中为 &lt;code&gt;1&lt;/code&gt; 的位置（即未来位置 &lt;code&gt;j &amp;gt; i&lt;/code&gt;）的分数，替换为 &lt;strong&gt;&lt;code&gt;-inf&lt;/code&gt;（负无穷）&lt;/strong&gt;。&lt;br&gt;
&lt;strong&gt;为什么是负无穷&lt;/strong&gt;：因为下一步要做 Softmax，&lt;code&gt;-inf&lt;/code&gt; 经过 &lt;code&gt;e^(-inf) = 0&lt;/code&gt;，这些位置的注意力权重就会变成绝对的 &lt;code&gt;0&lt;/code&gt;，相当于物理上切断视线。&lt;br&gt;
而对角线及以前的位置（&lt;code&gt;j ≤ i&lt;/code&gt;）原封不动，正常可见。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;缩放 + Softmax&lt;/strong&gt;：除以 &lt;code&gt;√d_k&lt;/code&gt; 后，对每一行做 Softmax。由于未来位置是 &lt;code&gt;0&lt;/code&gt;，权重只分配到当前及过去的位置上，总和依然为 1。此时第 &lt;code&gt;i&lt;/code&gt; 行的权重分布，只代表&amp;quot;我（第 i 个词）如何看待我和我之前的所有词&amp;quot;。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;加权求和 + 合并多头&lt;/strong&gt;：乘以 V，再拼接并过 &lt;code&gt;W_O&lt;/code&gt;，得到 &lt;code&gt;Masked_Attn_Out&lt;/code&gt;，形状 &lt;code&gt;(B, T, d_model)&lt;/code&gt;。&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;设计动机&lt;/strong&gt;：这是保证&amp;quot;自回归&amp;quot;特性的物理屏障。训练时虽然并行输入整句，但通过这个掩码，每个位置的梯度只依赖于前文，等效于逐个生成时的状态，让模型学会根据历史推未来。&lt;/p&gt;
&lt;hr&gt;
&lt;h4 id=&#34;步骤-32残差连接add&#34;&gt;步骤 3.2：残差连接（Add）
&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;做什么&lt;/strong&gt;：将原始输入 &lt;code&gt;X_dec&lt;/code&gt; 与 &lt;code&gt;Masked_Attn_Out&lt;/code&gt; 直接相加：&lt;code&gt;X_dec = X_dec + Masked_Attn_Out&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;作用&lt;/strong&gt;：与编码器相同，保留原始路径，防止自注意力层破坏已经学好的低层特征，同时提供梯度直通捷径。&lt;/p&gt;
&lt;hr&gt;
&lt;h4 id=&#34;步骤-33层归一化layernorm&#34;&gt;步骤 3.3：层归一化（LayerNorm）
&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;做什么&lt;/strong&gt;：对步骤 3.2 的相加结果在 &lt;code&gt;d_model&lt;/code&gt; 维度上做层归一化，得到新的 &lt;code&gt;X_dec&lt;/code&gt;，形状不变。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;作用&lt;/strong&gt;：稳定数据分布，为下一个子层（交叉注意力）提供标准化输入。&lt;/p&gt;
&lt;hr&gt;
&lt;h3 id=&#34;子层二交叉注意力子层cross-attention-sub-layer也叫编码器-解码器注意力&#34;&gt;子层二：交叉注意力子层（Cross-Attention Sub-layer，也叫编码器-解码器注意力）
&lt;/h3&gt;&lt;p&gt;本子层遵循&amp;quot;核心注意力 → Add → LayerNorm&amp;quot;的流程。&lt;strong&gt;这是解码器独有的核心模块&lt;/strong&gt;，也是连接编码器和解码器的关键纽带。&lt;/p&gt;
&lt;h4 id=&#34;步骤-34交叉注意力cross-attention&#34;&gt;步骤 3.4：交叉注意力（Cross-Attention）
&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;做什么&lt;/strong&gt;：用&lt;strong&gt;解码器自己的状态作为&amp;quot;查询（Query）&amp;quot;&lt;/strong&gt;，去&lt;strong&gt;编码器输出的 Memory 中&amp;quot;检索&amp;quot;最相关的源语言信息&lt;/strong&gt;，作为&amp;quot;键（Key）&amp;ldquo;和&amp;quot;值（Value）&amp;quot;。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;拆解动作&lt;/strong&gt;：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;生成 Q（来自解码器）&lt;/strong&gt;：将上一步归一化后的 &lt;code&gt;X_dec&lt;/code&gt;（形状 &lt;code&gt;(B, T, d_model)&lt;/code&gt;）乘以当前子层的&lt;code&gt;W_Q&lt;/code&gt;，得到 Q，形状 &lt;code&gt;(B, T, d_model)&lt;/code&gt;。这里的 Q 代表&amp;quot;我想问编码器什么问题&amp;rdquo;。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;生成 K 和 V（来自编码器）&lt;/strong&gt;：将编码器最终记忆 &lt;code&gt;Encoder_Memory&lt;/code&gt;（形状 &lt;code&gt;(B, S, d_model)&lt;/code&gt;）分别乘以当前子层的&lt;code&gt;W_K&lt;/code&gt; 和 &lt;code&gt;W_V&lt;/code&gt;（注意这两组权重与子层一中的 &lt;code&gt;W_K, W_V&lt;/code&gt; 是&lt;strong&gt;独立不共享&lt;/strong&gt;的），得到 K 和 V，形状均为 &lt;code&gt;(B, S, d_model)&lt;/code&gt;。&lt;br&gt;
这里的 K 代表&amp;quot;源语言所有位置的特征索引&amp;quot;，V 代表&amp;quot;源语言所有位置的具体内容值&amp;quot;。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;拆分为多头&lt;/strong&gt;：Q 拆为 &lt;code&gt;(B, h, T, d_k)&lt;/code&gt;，K 和 V 拆为 &lt;code&gt;(B, h, S, d_k)&lt;/code&gt;。注意 &lt;strong&gt;Q 的长度是 T（目标长度），K/V 的长度是 S（源长度）&lt;/strong&gt;，二者不一定相等！&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;计算注意力分数&lt;/strong&gt;：&lt;code&gt;Q × K^T&lt;/code&gt;，得到分数矩阵形状 &lt;code&gt;(B, h, T, S)&lt;/code&gt;。&lt;br&gt;
矩阵中第 &lt;code&gt;i&lt;/code&gt; 行第 &lt;code&gt;j&lt;/code&gt; 列，表示&lt;strong&gt;目标语言第 i 个位置，对源语言第 j 个位置的关注强度&lt;/strong&gt;。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;缩放 + Softmax&lt;/strong&gt;：除以 &lt;code&gt;√d_k&lt;/code&gt; 后，对分数的&lt;strong&gt;每一行&lt;/strong&gt;（即每个目标位置）做 Softmax。&lt;strong&gt;这里没有任何掩码&lt;/strong&gt;，因为目标语言的某个词去关注源语言的任意位置（无论前后）都是合理且允许的，不存在&amp;quot;泄露未来&amp;quot;的问题（源语言全文已知）。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;加权求和 + 合并多头&lt;/strong&gt;：将权重矩阵与 V（源语言内容）相乘，得到 &lt;code&gt;(B, h, T, d_k)&lt;/code&gt;，再拼接并过输出投影 &lt;code&gt;W_O&lt;/code&gt;，得到 &lt;code&gt;Cross_Attn_Out&lt;/code&gt;，形状 &lt;code&gt;(B, T, d_model)&lt;/code&gt;。&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;设计动机&lt;/strong&gt;：这一步本质是&amp;quot;软对齐&amp;quot;（Soft Alignment）。比如翻译英译中，当解码器生成&amp;quot;苹果&amp;quot;这个词时，Q 会去 K 里检索&amp;quot;apple&amp;quot;，并给对应位置的 V 分配高权重，从而把&amp;quot;apple&amp;quot;的语义特征提取过来。它让解码器在生成每一个目标词时，都能动态地、有选择性地翻阅整本源语言&amp;quot;词典&amp;quot;。&lt;/p&gt;
&lt;hr&gt;
&lt;h4 id=&#34;步骤-35残差连接add&#34;&gt;步骤 3.5：残差连接（Add）
&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;做什么&lt;/strong&gt;：将子层一的输出（即步骤 3.3 归一化后的 &lt;code&gt;X_dec&lt;/code&gt;）与 &lt;code&gt;Cross_Attn_Out&lt;/code&gt; 直接相加：&lt;code&gt;X_dec = X_dec + Cross_Attn_Out&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;设计逻辑&lt;/strong&gt;：这里的 &lt;code&gt;X_dec&lt;/code&gt; 代表着&amp;quot;我基于前文对自己的理解&amp;quot;，现在加上从源语言检索来的外部信息，实现了&amp;quot;内外结合&amp;quot;。&lt;/p&gt;
&lt;hr&gt;
&lt;h4 id=&#34;步骤-36层归一化layernorm&#34;&gt;步骤 3.6：层归一化（LayerNorm）
&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;做什么&lt;/strong&gt;：对步骤 3.5 的相加结果做层归一化，得到新的 &lt;code&gt;X_dec&lt;/code&gt;，形状 &lt;code&gt;(B, T, d_model)&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;作用&lt;/strong&gt;：为最后一个子层（FFN）提供稳定输入。&lt;/p&gt;
&lt;hr&gt;
&lt;h3 id=&#34;子层三前馈网络子层feed-forward-network-sub-layer&#34;&gt;子层三：前馈网络子层（Feed-Forward Network Sub-layer）
&lt;/h3&gt;&lt;p&gt;本子层遵循&amp;quot;核心 FFN → Add → LayerNorm&amp;quot;的流程。&lt;/p&gt;
&lt;h4 id=&#34;步骤-37前馈网络ffn&#34;&gt;步骤 3.7：前馈网络（FFN）
&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;做什么&lt;/strong&gt;：和编码器完全相同，对每个位置独立做非线性变换，加深特征抽象。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;公式&lt;/strong&gt;：&lt;code&gt;FFN(X) = ReLU(X * W_1 + b_1) * W_2 + b_2&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;张量变化&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;第一层：&lt;code&gt;X_dec (B, T, 512)&lt;/code&gt; → &lt;code&gt;(B, T, 2048)&lt;/code&gt;，维度扩至 4 倍。&lt;/li&gt;
&lt;li&gt;ReLU 激活，引入非线性。&lt;/li&gt;
&lt;li&gt;第二层：&lt;code&gt;(B, T, 2048)&lt;/code&gt; → &lt;code&gt;(B, T, 512)&lt;/code&gt;，投影回原维度。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;关键点&lt;/strong&gt;：它和交叉注意力输入是串联关系，而不是并联。先&amp;quot;查资料&amp;quot;（交叉注意力），再&amp;quot;写总结&amp;quot;（FFN），逻辑上非常顺畅。此时 FFN 同时基于&amp;quot;我已经写了什么（历史）&amp;ldquo;和&amp;quot;源语言说了什么（对齐信息）&amp;ldquo;来做统一的深度加工。&lt;/p&gt;
&lt;hr&gt;
&lt;h4 id=&#34;步骤-38残差连接add&#34;&gt;步骤 3.8：残差连接（Add）
&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;做什么&lt;/strong&gt;：将子层二的输出（即步骤 3.6 归一化后的 &lt;code&gt;X_dec&lt;/code&gt;）与 &lt;code&gt;FFN_Out&lt;/code&gt; 直接相加：&lt;code&gt;X_dec = X_dec + FFN_Out&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;作用&lt;/strong&gt;：给 FFN 层提供梯度直通捷径，确保即使 FFN 学习效果不佳，信息仍能无损传递。&lt;/p&gt;
&lt;hr&gt;
&lt;h4 id=&#34;步骤-39层归一化layernorm&#34;&gt;步骤 3.9：层归一化（LayerNorm）
&lt;/h4&gt;&lt;p&gt;&lt;strong&gt;做什么&lt;/strong&gt;：对步骤 3.8 的相加结果做层归一化，输出本解码器层的最终结果，形状 &lt;code&gt;(B, T, d_model)&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;至此，一个解码器层的全部计算结束。&lt;/p&gt;
&lt;hr&gt;
&lt;h3 id=&#34;本层完成后的流向与跨层细节&#34;&gt;本层完成后的流向与跨层细节
&lt;/h3&gt;&lt;p&gt;这一层的输出会作为&lt;strong&gt;下一层解码器&lt;/strong&gt;的输入，重复上述 6 次。注意：&lt;strong&gt;每一层解码器在做交叉注意力时，用的都是同一份 Encoder_Memory（来自顶层编码器）&lt;/strong&gt;，但每层都有自己独立可学习的 &lt;code&gt;W_Q, W_K, W_V&lt;/code&gt; 投影矩阵，所以每层对源语言的&amp;quot;检索方式&amp;quot;可以不同——浅层可能关注词性，深层可能关注语义逻辑。&lt;/p&gt;
&lt;hr&gt;
&lt;h3 id=&#34;最终输出与生成6-层全部结束后&#34;&gt;最终输出与生成（6 层全部结束后）
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;第 6 层解码器输出形状 &lt;code&gt;(B, T, d_model)&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;将其送入一个&lt;strong&gt;线性层（Linear）&lt;/strong&gt;，将维度从 &lt;code&gt;d_model&lt;/code&gt; 映射到 &lt;strong&gt;目标词表大小 &lt;code&gt;V_tgt&lt;/code&gt;&lt;/strong&gt;（比如 3 万），得到 &lt;code&gt;(B, T, V_tgt)&lt;/code&gt; 的 logits 分数。&lt;/li&gt;
&lt;li&gt;最后接一个 &lt;strong&gt;Softmax&lt;/strong&gt;，将分数转为概率分布，选择概率最高的词作为当前时刻的预测输出。&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h3 id=&#34;额外补充训练与推理时的关键差异&#34;&gt;额外补充：训练与推理时的关键差异
&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;训练时（Teacher Forcing）&lt;/strong&gt;：目标整句已知，一次性输入完整偏移序列，利用掩码实现&lt;strong&gt;并行计算&lt;/strong&gt;，所有位置的 Loss 同时算出，效率极高。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;推理时（自回归生成）&lt;/strong&gt;：只能输入 &lt;code&gt;&amp;lt;BOS&amp;gt;&lt;/code&gt;，生成第一个词；然后把第一个词拼进输入，生成第二个词；以此类推，直到生成 &lt;code&gt;&amp;lt;EOS&amp;gt;&lt;/code&gt;。&lt;strong&gt;每生成一个词，都要重新跑一遍整个解码器前向传播&lt;/strong&gt;（但缓存机制 K/V Cache 可大幅加速，这是工程优化的重点）。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;第四阶段输出生成output-projection&#34;&gt;第四阶段：输出生成（Output Projection）
&lt;/h2&gt;&lt;p&gt;解码器6层全部跑完后：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;得到最终隐藏状态 &lt;code&gt;X_final&lt;/code&gt;（形状 &lt;code&gt;B, T_target, d_model&lt;/code&gt;）。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;线性投影（Linear）&lt;/strong&gt;：&lt;code&gt;Logits = X_final * W_Proj^T&lt;/code&gt;，将 &lt;code&gt;d_model&lt;/code&gt; 映射回 &lt;strong&gt;目标词表大小（Vocab_Target）&lt;/strong&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Softmax&lt;/strong&gt;：对 &lt;code&gt;Logits&lt;/code&gt; 沿最后一维取 Softmax，得到每个位置的概率分布。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;取最高概率&lt;/strong&gt;：&lt;code&gt;Predicted_Token = argmax(Logits)&lt;/code&gt;，得到当前步预测的词。&lt;/li&gt;
&lt;/ol&gt;
&lt;hr&gt;
&lt;h2 id=&#34;第五阶段训练时的掩码mask详解极易混淆&#34;&gt;第五阶段：训练时的掩码（Mask）详解（极易混淆）
&lt;/h2&gt;&lt;table&gt;
  &lt;thead&gt;
      &lt;tr&gt;
          &lt;th style=&#34;text-align: left&#34;&gt;层的位置&lt;/th&gt;
          &lt;th style=&#34;text-align: left&#34;&gt;掩码类型&lt;/th&gt;
          &lt;th style=&#34;text-align: left&#34;&gt;具体操作&lt;/th&gt;
      &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
      &lt;tr&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;编码器自注意力&lt;/strong&gt;&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;无掩码&lt;/strong&gt;（Padding Mask除外）&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;所有词可以看到所有词。例如 &lt;code&gt;[我, 爱, 你]&lt;/code&gt; 中，&amp;ldquo;爱&amp;quot;可以同时看到&amp;quot;我&amp;quot;和&amp;quot;你&amp;rdquo;。&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;解码器掩码自注意力&lt;/strong&gt;&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;因果掩码 + Padding掩码&lt;/strong&gt;&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;code&gt;[&amp;lt;SOS&amp;gt;, I, love, you]&lt;/code&gt; 中，&amp;ldquo;love&amp;quot;只能看到 &lt;code&gt;&amp;lt;SOS&amp;gt;&lt;/code&gt; 和 &lt;code&gt;I&lt;/code&gt;，看不到 &lt;code&gt;you&lt;/code&gt;（上三角设为 &lt;code&gt;-inf&lt;/code&gt;）。&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;解码器交叉注意力&lt;/strong&gt;&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;仅 Padding掩码&lt;/strong&gt;（无因果掩码）&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;Q（目标端）可以无条件地查看 K/V（源端）的所有位置。源端全部可见，不需要遮罩未来（因为源端没有&amp;quot;未来&amp;quot;概念）。&lt;/td&gt;
      &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;
&lt;hr&gt;
&lt;h2 id=&#34;第六阶段训练目标与损失loss&#34;&gt;第六阶段：训练目标与损失（Loss）
&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;标签&lt;/strong&gt;：原始目标句子（未右移的 Ground Truth）。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;损失函数&lt;/strong&gt;：&lt;strong&gt;交叉熵损失（Cross-Entropy Loss）&lt;/strong&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;计算方式&lt;/strong&gt;：仅计算目标句子有效词元（忽略 &lt;code&gt;&amp;lt;PAD&amp;gt;&lt;/code&gt;）的负对数似然。不涉及解码器输出端的任何额外辅助损失（原版没有MTP）。&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2 id=&#34;第七阶段推理inference时的解码差异&#34;&gt;第七阶段：推理（Inference）时的解码差异
&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;训练&lt;/strong&gt;：并行输入完整的 &lt;code&gt;[&amp;lt;SOS&amp;gt;, 词1, 词2, ...]&lt;/code&gt;（Teacher Forcing）。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;推理&lt;/strong&gt;：无法并行。解码器输入只有 &lt;code&gt;&amp;lt;SOS&amp;gt;&lt;/code&gt;，输出第一个词；然后将 &lt;code&gt;&amp;lt;SOS&amp;gt;&lt;/code&gt; + 第一个词作为新输入，再输出第二个词……&lt;strong&gt;循环往复（自回归生成）&lt;/strong&gt;，直到输出结束符 &lt;code&gt;&amp;lt;EOS&amp;gt;&lt;/code&gt;。&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2 id=&#34;附录原版标准transformer-vs-现代大模型你之前问的核心差异对照表&#34;&gt;附录：原版标准Transformer VS 现代大模型（你之前问的）核心差异对照表
&lt;/h2&gt;&lt;table&gt;
  &lt;thead&gt;
      &lt;tr&gt;
          &lt;th style=&#34;text-align: left&#34;&gt;组件&lt;/th&gt;
          &lt;th style=&#34;text-align: left&#34;&gt;&lt;strong&gt;标准原版 Transformer&lt;/strong&gt;&lt;/th&gt;
          &lt;th style=&#34;text-align: left&#34;&gt;&lt;strong&gt;现代 GPT/DeepSeek（你之前的笔记）&lt;/strong&gt;&lt;/th&gt;
      &lt;/tr&gt;
  &lt;/thead&gt;
  &lt;tbody&gt;
      &lt;tr&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;架构类型&lt;/strong&gt;&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;编码器-解码器（双向+自回归）&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;仅解码器（纯自回归）&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;位置编码&lt;/strong&gt;&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;Sin/Cos 加法&lt;/strong&gt;（固定）&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;RoPE 旋转乘法&lt;/strong&gt;（相对位置）&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;归一化顺序&lt;/strong&gt;&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;Post-Norm&lt;/strong&gt;（Add -&amp;gt; Norm）&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;Pre-Norm&lt;/strong&gt;（Norm -&amp;gt; Add）&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;归一化函数&lt;/strong&gt;&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;LayerNorm（带均值中心化）&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;RMSNorm（仅缩放，无均值）&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;前馈激活&lt;/strong&gt;&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;ReLU&lt;/strong&gt;&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;SwiGLU / GeGLU&lt;/strong&gt;（门控线性）&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;注意力机制&lt;/strong&gt;&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;标准 MHA（Q/K/V全尺寸）&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;MLA&lt;/strong&gt;（低秩压缩，仅DeepSeek） 或标准MHA&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;层数设置&lt;/strong&gt;&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;编码器6层 + 解码器6层（对称）&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;仅解码器 80+ 层（极深非对称）&lt;/td&gt;
      &lt;/tr&gt;
      &lt;tr&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;&lt;strong&gt;位置交叉&lt;/strong&gt;&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;通过 Cross-Attention 强耦合&lt;/td&gt;
          &lt;td style=&#34;text-align: left&#34;&gt;无交叉注意力（仅在输入侧拼接上下文）&lt;/td&gt;
      &lt;/tr&gt;
  &lt;/tbody&gt;
&lt;/table&gt;
&lt;hr&gt;
&lt;h2 id=&#34;流程图速记文字版&#34;&gt;流程图速记（文字版）
&lt;/h2&gt;&lt;div class=&#34;highlight&#34;&gt;&lt;div class=&#34;chroma&#34;&gt;
&lt;table class=&#34;lntable&#34;&gt;&lt;tr&gt;&lt;td class=&#34;lntd&#34;&gt;
&lt;pre tabindex=&#34;0&#34; class=&#34;chroma&#34;&gt;&lt;code&gt;&lt;span class=&#34;lnt&#34;&gt; 1
&lt;/span&gt;&lt;span class=&#34;lnt&#34;&gt; 2
&lt;/span&gt;&lt;span class=&#34;lnt&#34;&gt; 3
&lt;/span&gt;&lt;span class=&#34;lnt&#34;&gt; 4
&lt;/span&gt;&lt;span class=&#34;lnt&#34;&gt; 5
&lt;/span&gt;&lt;span class=&#34;lnt&#34;&gt; 6
&lt;/span&gt;&lt;span class=&#34;lnt&#34;&gt; 7
&lt;/span&gt;&lt;span class=&#34;lnt&#34;&gt; 8
&lt;/span&gt;&lt;span class=&#34;lnt&#34;&gt; 9
&lt;/span&gt;&lt;span class=&#34;lnt&#34;&gt;10
&lt;/span&gt;&lt;span class=&#34;lnt&#34;&gt;11
&lt;/span&gt;&lt;span class=&#34;lnt&#34;&gt;12
&lt;/span&gt;&lt;span class=&#34;lnt&#34;&gt;13
&lt;/span&gt;&lt;span class=&#34;lnt&#34;&gt;14
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;
&lt;td class=&#34;lntd&#34;&gt;
&lt;pre tabindex=&#34;0&#34; class=&#34;chroma&#34;&gt;&lt;code class=&#34;language-text&#34; data-lang=&#34;text&#34;&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;源输入 [英] --&amp;gt; 嵌入+PosEnc --&amp;gt; 编码器(重复6次)  
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;                                ├─ 自注意力(无掩码)  
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;                                ├─ Add &amp;amp; Norm  
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;                                └─ FFN(ReLU) + Add &amp;amp; Norm  
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;                                     |
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;                                     | (输出作为 K, V)
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;                                     ↓
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;目标输入 [中] --&amp;gt; 嵌入+PosEnc --&amp;gt; 解码器(重复6次)  
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;                                ├─ 掩码自注意力(因果) + Add &amp;amp; Norm  
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;                                ├─ 交叉注意力(Q=解码器, K/V=编码器) + Add &amp;amp; Norm  
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;                                └─ FFN(ReLU) + Add &amp;amp; Norm  
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;                                     |
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;                                     ↓
&lt;/span&gt;&lt;/span&gt;&lt;span class=&#34;line&#34;&gt;&lt;span class=&#34;cl&#34;&gt;                                 线性层 + Softmax --&amp;gt; 输出中文词
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;
&lt;/div&gt;
&lt;/div&gt;</description>
        </item>
        <item>
        <title>Transformer K/V Cache 深度解析</title>
        <link>https://Yannick-Mo.github.io/p/transformer-k/v-cache-%E6%B7%B1%E5%BA%A6%E8%A7%A3%E6%9E%90/</link>
        <pubDate>Mon, 19 Jan 2026 14:14:00 +0800</pubDate>
        
        <guid>https://Yannick-Mo.github.io/p/transformer-k/v-cache-%E6%B7%B1%E5%BA%A6%E8%A7%A3%E6%9E%90/</guid>
        <description>&lt;p&gt;Transformer工程落地的核心痛点。K/V Cache（键值缓存）不仅是加速技巧，更是&lt;strong&gt;大模型推理系统设计的基石&lt;/strong&gt;。&lt;/p&gt;
&lt;hr&gt;
&lt;h3 id=&#34;第一步先看没有-cache时会发生什么灾难&#34;&gt;第一步：先看&amp;quot;没有 Cache&amp;quot;时，会发生什么灾难
&lt;/h3&gt;&lt;p&gt;在推理阶段（生成阶段），解码器是&lt;strong&gt;逐个蹦词&lt;/strong&gt;的。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;第 1 步&lt;/strong&gt;：输入 &lt;code&gt;[&amp;lt;BOS&amp;gt;]&lt;/code&gt;，生成第 1 个词 &lt;code&gt;w1&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;第 2 步&lt;/strong&gt;：输入 &lt;code&gt;[&amp;lt;BOS&amp;gt;, w1]&lt;/code&gt;，生成第 2 个词 &lt;code&gt;w2&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;第 3 步&lt;/strong&gt;：输入 &lt;code&gt;[&amp;lt;BOS&amp;gt;, w1, w2]&lt;/code&gt;，生成第 3 个词 &lt;code&gt;w3&lt;/code&gt;。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;如果不用 Cache&lt;/strong&gt;，第 3 步会把 &lt;code&gt;[&amp;lt;BOS&amp;gt;, w1, w2]&lt;/code&gt; &lt;strong&gt;整个序列&lt;/strong&gt;重新喂进解码器，重新计算 &lt;code&gt;w1&lt;/code&gt; 和 &lt;code&gt;w2&lt;/code&gt; 的 Q、K、V，重新跑一遍完整的注意力矩阵（形状 &lt;code&gt;T × T&lt;/code&gt;）。&lt;/p&gt;
&lt;p&gt;这就导致&lt;strong&gt;计算复杂度呈平方级增长（O(T²)）&lt;/strong&gt;。生成第 1000 个词时，前面 999 个词的 K/V 要被重复计算 999 次，这在生产中是&lt;strong&gt;完全不可接受的&lt;/strong&gt;（GPU 会冒烟）。&lt;/p&gt;
&lt;hr&gt;
&lt;h3 id=&#34;第二步引入-kv-cache-的核心逻辑用显存换时间&#34;&gt;第二步：引入 K/V Cache 的核心逻辑——用显存换时间
&lt;/h3&gt;&lt;p&gt;仔细看掩码自注意力的公式：&lt;code&gt;Attention(Q_t, K, V)&lt;/code&gt;。
当我们要生成&lt;strong&gt;当前第 &lt;code&gt;t&lt;/code&gt; 个位置&lt;/strong&gt;的新词时：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Q（查询）&lt;/strong&gt;：只由&lt;strong&gt;当前最新输入的 token&lt;/strong&gt; 计算得出，形状 &lt;code&gt;(B, 1, d_model)&lt;/code&gt;。只有&amp;quot;我&amp;quot;在提问。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;K（键）和 V（值）&lt;/strong&gt;：需要包括&lt;strong&gt;第 1 到第 &lt;code&gt;t&lt;/code&gt; 个位置&lt;/strong&gt;的所有 token，因为我要看过去的全部历史。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Cache 的骚操作来了&lt;/strong&gt;：
既然第 1 到第 &lt;code&gt;t-1&lt;/code&gt; 个位置的 K 和 V 在前面的步骤中已经算过了，且&lt;strong&gt;它们的内容永远不变&lt;/strong&gt;（因为自回归中过去的词不会变），那我何必重算？&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;做法&lt;/strong&gt;：在 GPU 显存中划出一块连续区域（即 K/V Cache），把每一层解码器计算出的历史 K 和 V 矩阵&lt;strong&gt;像拼积木一样存下来&lt;/strong&gt;。&lt;/p&gt;
&lt;hr&gt;
&lt;h3 id=&#34;第三步带-cache-的单步生成流程物理张量变化&#34;&gt;第三步：带 Cache 的单步生成流程（物理张量变化）
&lt;/h3&gt;&lt;p&gt;假设当前处于第 &lt;code&gt;t&lt;/code&gt; 步，解码器有 &lt;code&gt;L&lt;/code&gt; 层（原论文 6 层，现在大模型几十上百层）。&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;输入&lt;/strong&gt;：只有新生成的这一个 token，形状 &lt;code&gt;(B, 1)&lt;/code&gt;（词嵌入后变为 &lt;code&gt;(B, 1, d_model)&lt;/code&gt;）。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;第 1 层掩码自注意力&lt;/strong&gt;：
&lt;ul&gt;
&lt;li&gt;根据这个新 token 的 &lt;code&gt;X&lt;/code&gt;，计算它的 &lt;code&gt;Q_new&lt;/code&gt;、&lt;code&gt;K_new&lt;/code&gt;、&lt;code&gt;V_new&lt;/code&gt;，形状均为 &lt;code&gt;(B, 1, d_head)&lt;/code&gt;（按头算）。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;读取 Cache&lt;/strong&gt;：从显存中取出该层之前存好的历史 K 和 V，形状为 &lt;code&gt;(B, t-1, d_head)&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;拼接（关键物理动作）&lt;/strong&gt;：将 &lt;code&gt;K_new&lt;/code&gt; 拼到历史 K 后面，得到 &lt;code&gt;K_all = [K_history | K_new]&lt;/code&gt;，形状 &lt;code&gt;(B, t, d_head)&lt;/code&gt;。V 同理。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;计算注意力&lt;/strong&gt;：用 &lt;code&gt;Q_new&lt;/code&gt;（形状 &lt;code&gt;B, 1, d&lt;/code&gt;）去点乘 &lt;code&gt;K_all&lt;/code&gt;（形状 &lt;code&gt;B, t, d&lt;/code&gt;），得到分数 &lt;code&gt;(B, 1, t)&lt;/code&gt;。此时矩阵的&lt;strong&gt;长度刚好是 &lt;code&gt;t&lt;/code&gt;&lt;/strong&gt;，只包含当前及过去位置。因为 Q 只有一个向量，天然就不存在偷看未来的可能性，&lt;strong&gt;连上三角掩码矩阵都不需要生成了&lt;/strong&gt;（节约了计算量）。&lt;/li&gt;
&lt;li&gt;加权求和后得到当前层的输出 &lt;code&gt;(B, 1, d)&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;更新 Cache&lt;/strong&gt;：将计算出的 &lt;code&gt;K_new&lt;/code&gt; 和 &lt;code&gt;V_new&lt;/code&gt; &lt;strong&gt;写回&lt;/strong&gt;显存中该层的缓存区。此时 Cache 长度变为 &lt;code&gt;t&lt;/code&gt;。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;经过交叉注意力、FFN&lt;/strong&gt;（交叉注意力的 K/V 来自 Encoder，全程固定，只需存一份，不参与这里的缓存增长）。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;循环 L 层&lt;/strong&gt;，最终输出 logits，预测下一个词。&lt;/li&gt;
&lt;/ol&gt;
&lt;hr&gt;
&lt;h3 id=&#34;第四步量化-cache-带来的恐怖显存占用&#34;&gt;第四步：量化 Cache 带来的&amp;quot;恐怖&amp;quot;显存占用
&lt;/h3&gt;&lt;p&gt;Cache 并非免费午餐，它的代价是&lt;strong&gt;显存墙&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;假设一个 70B 参数的模型（如 LLaMA 2 70B）：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;层数 &lt;code&gt;L = 80&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;隐藏维度 &lt;code&gt;d_model = 8192&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;多头数 &lt;code&gt;h = 64&lt;/code&gt;，每头维度 &lt;code&gt;d_k = 128&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;生成序列长度 &lt;code&gt;T = 4096&lt;/code&gt;，批次 &lt;code&gt;B = 32&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;计算单份 K/V Cache 的内存大小（FP16，每个数占 2 字节）&lt;/strong&gt;：
公式是：&lt;code&gt;Cache_Memory = 2（K和V两份） × B × T × L × d_model&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;代入数值：&lt;code&gt;2 × 32 × 4096 × 80 × 8192&lt;/code&gt; 字节。
我们算一下：&lt;code&gt;2 × 32 = 64&lt;/code&gt;；&lt;code&gt;64 × 4096 = 262,144&lt;/code&gt;；&lt;code&gt;262,144 × 80 = 20,971,520&lt;/code&gt;；&lt;code&gt;20,971,520 × 8192 ≈ 171,798,691,840&lt;/code&gt; 字节 ≈ &lt;strong&gt;160 GB&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;结论&lt;/strong&gt;：仅仅是存 K/V，就要吃掉 &lt;strong&gt;160 GB 显存&lt;/strong&gt;。而模型权重本身还有 140 GB 左右。&lt;strong&gt;显存瓶颈远大于算力瓶颈&lt;/strong&gt;——这也是为什么你经常看到大模型推理时，GPU 算力利用率（MFU）很低，因为它在&amp;quot;等数据从显存搬运到计算单元&amp;quot;，时间都花在 I/O 上了。&lt;/p&gt;
&lt;hr&gt;
&lt;h3 id=&#34;第五步工业级的高级优化技巧这才是真正的深入&#34;&gt;第五步：工业级的高级优化技巧（这才是真正的&amp;quot;深入&amp;quot;）
&lt;/h3&gt;&lt;p&gt;基础 Cache 只是开始，工业界为了解决上面的显存和带宽问题，发展出了极其精妙的优化手段：&lt;/p&gt;
&lt;h4 id=&#34;1-分页注意力pagedattention--vllm-的核心&#34;&gt;1. 分页注意力（PagedAttention —— vLLM 的核心）
&lt;/h4&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;痛点&lt;/strong&gt;：传统 Cache 必须在显存中申请&lt;strong&gt;连续&lt;/strong&gt;的内存空间。但每个请求生成的序列长度不一，且提前未知，导致大量显存碎片（Internal Fragmentation）。比如预分配 2048 长度，实际只生成了 128，剩下 1920 的长度就白白浪费了。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;解决方案&lt;/strong&gt;：像操作系统的虚拟内存一样，把 K/V Cache 切成固定大小的&amp;quot;页&amp;quot;（Page，比如每页存 16 个 token 的 K/V）。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;好处&lt;/strong&gt;：物理上不连续没关系，逻辑上通过页表（Block Table）索引。空间按需分配，&lt;strong&gt;碎片率降低 90% 以上&lt;/strong&gt;。同时支持同一个 Prompt 在多个请求中共享 Cache（比如系统提示词），极其适合高并发场景。&lt;/li&gt;
&lt;/ul&gt;
&lt;h4 id=&#34;2-连续批处理continuous-batching又称动态批处理&#34;&gt;2. 连续批处理（Continuous Batching，又称动态批处理）
&lt;/h4&gt;&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;痛点&lt;/strong&gt;：传统静态批处理（Static Batching）必须等整个批次中最长的那个序列生成完，所有请求才能一起结束。短序列生成了 10 个词，但要干等长序列生成 100 个词，GPU 在干等时是空闲的。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;解决方案&lt;/strong&gt;：每次迭代（生成一个 token 后），检查哪些请求已经生成了 &lt;code&gt;&amp;lt;EOS&amp;gt;&lt;/code&gt;，立即把它们&amp;quot;踢出&amp;quot;批次，空出来的显存位置立刻塞入新的请求。这使得 &lt;strong&gt;GPU 利用率提升 2-4 倍&lt;/strong&gt;。&lt;/li&gt;
&lt;/ul&gt;
&lt;h4 id=&#34;3-kv-缓存量化kv-cache-quantization如-fp8--int4&#34;&gt;3. KV 缓存量化（KV Cache Quantization，如 FP8 / INT4）
&lt;/h4&gt;&lt;ul&gt;
&lt;li&gt;注意力分数对 K/V 的精度异常敏感（尤其 V 负责内容，精度不够会胡言乱语），但研究发现可以针对 K 和 V 使用不同的精度策略。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;典型做法&lt;/strong&gt;：K 缓存保持较高精度（如 FP16），V 缓存压到 INT8 甚至 4-bit。因为 K 主要负责&amp;quot;找位置&amp;quot;，精度低一点会丢失细微匹配，但 V 负责&amp;quot;取内容&amp;quot;，冗余度稍高。通过这种方式，&lt;strong&gt;可将 160 GB 压缩至 50 GB 以下&lt;/strong&gt;。&lt;/li&gt;
&lt;/ul&gt;
&lt;h4 id=&#34;4-flashdecoding--flashattention-的配合&#34;&gt;4. FlashDecoding / FlashAttention 的配合
&lt;/h4&gt;&lt;ul&gt;
&lt;li&gt;当 Cache 长度变长（比如处理 10 万上下文），序列维度 &lt;code&gt;t&lt;/code&gt; 极大。单靠一个 GPU 核心算 &lt;code&gt;Q × K_all^T&lt;/code&gt;（形状 &lt;code&gt;1 × 100k&lt;/code&gt;）依然很慢。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;优化&lt;/strong&gt;：将长序列的 Cache 切分成小块，交给不同的 GPU 线程块（Thread Block）并行计算局部分数，最后在全局做一次归约（Reduce Sum）。这解决了长上下文场景下的&amp;quot;内存带宽墙&amp;quot;问题。&lt;/li&gt;
&lt;/ul&gt;
&lt;h4 id=&#34;5-投机采样speculative-decoding中的-cache-处理&#34;&gt;5. 投机采样（Speculative Decoding）中的 Cache 处理
&lt;/h4&gt;&lt;ul&gt;
&lt;li&gt;这是目前最火的加速推理范式。用小模型（Draft Model）快速生成 5-6 个候选 token，大模型（Target Model）并行验证。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;这里的 Cache 讲究&amp;quot;回滚&amp;quot;&lt;/strong&gt;：如果验证失败，大模型需要&lt;strong&gt;丢弃&lt;/strong&gt;这 5-6 个 token 对应的 K/V，并将 Cache 指针恢复至验证前的位置（通过维护版本号或逻辑长度指针实现，不物理擦除数据）。&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h3 id=&#34;第六步一个极易混淆的交叉注意力补充说明&#34;&gt;第六步：一个极易混淆的&amp;quot;交叉注意力&amp;quot;补充说明
&lt;/h3&gt;&lt;p&gt;在解码器的**交叉注意力（Cross-Attention）**层，K 和 V 来自编码器的 Memory。
这块 Cache 的处理方式完全不同：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;因为源句子是固定的，Encoder_Memory 在解码开始前&lt;strong&gt;一次性算好&lt;/strong&gt;，存入显存。&lt;/li&gt;
&lt;li&gt;在生成整个目标句子的过程中，交叉注意力层的 K 和 V &lt;strong&gt;不会随着 &lt;code&gt;t&lt;/code&gt; 增长而增长&lt;/strong&gt;，它们永远是固定的 &lt;code&gt;(B, S_src, d)&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;因此，这里的&amp;quot;Cache&amp;quot;更像是一个只读的全局共享变量，不存在&amp;quot;拼接&amp;quot;操作，也基本不占用增长性的显存开销。&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h3 id=&#34;总结一句核心本质&#34;&gt;总结一句核心本质
&lt;/h3&gt;&lt;p&gt;&lt;strong&gt;K/V Cache 的本质是用显存（空间）换取时间&lt;/strong&gt;，将自回归生成从 &lt;strong&gt;O(T²)&lt;/strong&gt; 的时间复杂度降为 &lt;strong&gt;O(T)&lt;/strong&gt;（只增加一个 token 的长度）。而现代工程优化的全部努力——分页、量化、连续批处理——都是在&lt;strong&gt;对抗显存碎片和带宽限制&lt;/strong&gt;，让这张&amp;quot;空间换时间&amp;quot;的赌局能玩得转。&lt;/p&gt;
</description>
        </item>
        
    </channel>
</rss>
